void bubbleSort(int*h,unsigned int len)
{
     int i,j,temp;
      for(i=0;i<len;i++)
           {
		for(j=i+1;j<len;j++)
		{
		      if(*(h+i)>*(h+j))
			{
			  temp=*(h+i);
			  *(h+i)=*(h+j);
			   *(h+j)=temp;
			}
		}
	   }
}
